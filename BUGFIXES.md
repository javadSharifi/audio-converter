# Bug fix log

Keep every row concise. Bug, Root Cause, and Fix should each fit on one line.

| Date | Bug | Root Cause | Fix | Files |
| ---- | --- | ---------- | --- | ----- |
| 2026-09-16 | Song end sometimes never advances; corrupt file or queue end froze player on stale "playing" | Single `ended` listener with no fallback/guard; failures only cleared `isPlaying`; silent early-return at queue end | Guarded redundant end detection (ended+watchdog, single-fire token), `resolveNextTrack` with explicit stop, session skip-set + notice for unplayable files | `musicPlayer/autoAdvance.ts` (new), `audioEngine.ts`, `useMusicPlayerStore.ts`, `i18n/en.ts`, `i18n/fa.ts` |
| 2026-09-16 | Booster % felt fake (200%≈other apps' 100%) + scrubbing crackled louder under boost | Every boost change fired 2 gain IPCs into one `setGainMb` (2nd overwrote 1st); scrub bursts fired overlapping ExoPlayer seeks; file Manual clamped to 200% vs live 400% | Single dB-honest gain IPC, trailing ~1s seek coalescing in store (optimistic UI kept), Manual unified to 0–400% with `alimiter` ceiling intact | `musicPlayer/audioEngine.ts`, `musicPlayer/seekDebounce.ts` (new), `useMusicPlayerStore.ts`, `processing/sound_booster/presets.rs` |
| 2026-09-16 | Dragging the boost slider chopped/stuttered audio (every tick re-applied full gain incl. IPC fan-out + enhancer reconvergence) | `setVolumeGainPercent` called `applyGainPercent` synchronously per pointer tick | Leading + ~150 ms throttled glide with trailing flush (`gainGlide.ts`); UI number stays per-tick, persist follows applied values | `musicPlayer/gainGlide.ts` (new), `useMusicPlayerStore.ts` |
| 2026-09-16 | Mobile wizard stepper (۱-۲-۳-۴) labels ellipsized to "..." (4 truncated items cramped in one row) | `truncate` on every label at `flex-1` widths below `sm` | Mobile shows current step only (badge + full label, no truncate); 4-step row kept `sm:` and up | `converter-wizard/WizardStepper.tsx` |
| 2026-09-16 | Theme circular reveal never played; toggle always switched instantly on all platforms | `startViewTransition` extracted and called unbound → native "Illegal invocation" → swallowed by fallback catch; mocks hid it | `.bind(document)` on the starter + regression test asserting `document` receiver | `utils/themeTransition.ts`, `utils/themeTransition.test.ts` |
| 2026-09-19 | Tab switches felt slow: covers re-extracted, lists re-rendered in floods | Artwork LRU only 100 + desktop cache in OS temp (purged between sessions) + rows subscribed to whole Sets/`currentTrack` + sub-second `currentTime` writes 4×/s + O(n×keys) album grouping + unvirtualized album detail | LRU→500 + persisted localStorage manifest (negative caching); durable OS cache dir w/ lazy temp migration; derived `playingKey` + per-row boolean selectors; 1s currentTime quantization; Map-indexed `computeAllAlbums`; virtualized `AlbumDetailView` | `utils/artwork.ts`, `music_library/artwork.rs`, `trackUtils.ts`, `useMusicPlayerStore.ts`, `audioEngine.ts`, `TrackRow.tsx`, `AlbumCard.tsx`, `TrackListView.tsx`, `AlbumDetailView.tsx`, `perfCaching.test.ts` (new) |
| 2026-09-19 | 400% volume boost felt weak compared to competitors; offline manual 400% choked under limiter | Android LoudnessEnhancer mapped only to 1204 mB (+12dB) while competitors use up to 8000 mB; offline manual 4x volume pushed audio into limiter pumping; GainSlider was capped at 200% | Mapped 101-400% to 0-8000 mB (matching reference competitor BoostEngine); offline manual uses dynaudnorm + alimiter; unified GainSlider to 400% | `musicPlayer/audioEngine.ts`, `sound_booster/presets.rs`, `file-booster/GainSlider.tsx`, `i18n/en.ts`, `i18n/fa.ts` |
| 2026-09-19 | Speaker protection button muted/lowered phone hardware volume by 70% + in-player booster had dated cramped UI | Button invoked androidApplyReduceHurt cutting AudioManager.STREAM_MUSIC to 30%; popup was cramped with auto-dismiss on click | Decoupled speaker protection from hardware stream volume (resets gain to 100% only); extracted fluid TrackBoosterSheet with dynamic tiers and live auditioning | `components/music-player/TrackBoosterSheet.tsx` (new), `components/music-player/NowPlayingView.tsx`, `android/AudioStreamManager.kt` |
| 2026-09-19 | In-player booster had unwanted subtitle hint text and static slider thumb | Audition hint text cluttered header; range slider lacked fluid capsule track and dynamic tooltip during interaction | Removed audition hint subtitle; redesigned slider to fluid capsule track with indicator bar and dynamic floating tooltip visible strictly during interaction | `components/music-player/TrackBoosterSheet.tsx`, `components/music-player/HighBoostSafetyModal.tsx` |
| 2026-09-19 | Albums tab showed count (3) but blank grid until tab switch | `AlbumGridVirtualized` had no `initialRect`/fallback, so first measure on lazy mount + async scan yielded 0 rows until a resize forced re-measure (Songs tab already had the guard) | Added `initialRect` + sync fallback rows/size + `measure()` on row-count change and ResizeObserver on scroller | `components/music-player/AlbumGridVirtualized.tsx` |
| 2026-09-19 | Converter mobile edit modal hidden behind bottom nav + trim preview stalled on last segment | MobileEditModal used z-50 < MusicPlayerNav z-[55]; TrimEditor onAudioTimeUpdate falsely paused during async seek before playhead settled | Elevate MobileEditModal to z-[80] (extracted file); latch preview window with hasEnteredRangeRef and WebKit zero-reset guard; migrate preview to 5s with sub-waveform hint | `components/MobileEditModal.tsx` (new), `components/FileList.tsx`, `components/TrimEditor.tsx`, `i18n/en.ts`, `i18n/fa.ts`, `components/__tests__/MobileEditModal.test.tsx` (new), `components/__tests__/TrimEditor.test.tsx` |
| 2026-09-19 | Onboarding gate background showed underlying tracks + required vertical scrolling on mobile | `OnboardingGate` used translucent `bg-zinc-100/95` / `GateFallback` returned `<></>`; oversized card padding caused viewport overflow | Made backdrop 100% opaque with ambient glow; compacted mobile section padding and button heights to fit within standard mobile viewports without scroll | `components/onboarding/OnboardingGate.tsx`, `App.tsx`, `components/onboarding/*.tsx` |
| 2026-09-20 | Onboarding restyled to match dark mock in dark and light modes (user rejected vendor-grey UI) | Pending visual QA on notch + bright/light fonts before confirming receipt against mock | Rebuilt shell/sections per mock (EQ emblem, layered ambient bg, glass cards, gradient CTA + shine, new i18n keys, `onboard-ripple`) with light surfaces + verified on Pixel_6_API_34 emulator install | `components/onboarding/OnboardingGate.tsx`, `PermissionSection.tsx`, `ThemeSection.tsx`, `LanguageSection.tsx`, `PerformanceSection.tsx`, `src/index.css`, `src/i18n/en.ts`, `src/i18n/fa.ts`, `__tests__/*` |
| 2026-09-20 | Onboarding asked for photos/videos permission on startup + showed "permission denied" toast when granting audio | Auto-prompted all permissions in MainActivity.onCreate(); bundled READ_MEDIA_VIDEO with AUDIO so denying video treated audio as denied | Removed onCreate prompt; decoupled audio (onboarding/player) from video (converter only); eliminated false denied toast; added focus check in PermissionSection | `MainActivity.kt`, `commands/mod.rs`, `lib.rs`, `PermissionSection.tsx`, `ConverterWizard.tsx`, `DropZone.tsx`, `FileList.tsx`, `tauri.ts` |
| 2026-09-20 | Video/photo permission prompt showed on onboarding startup | ConverterWizard mounted eagerly in KeepAlivePane and called requestVideoPermissions on mount regardless of active tool | Gated video permission check/request strictly on `activeTool === 'converter' && isFirstRunDone()` | `components/converter-wizard/ConverterWizard.tsx` |
| 2026-09-20 | First run required manual permission tap and had skip button; Converter lacked notice if video permission denied | User requested instant audio permission dialog on first start, removal of skip button, and clear banner in Converter when video access is missing | Added auto-prompt on mount in `OnboardingGate.tsx`, visually hid skip button in `PermissionSection.tsx`, added banner with action/dismiss in `WizardUploadStep.tsx` | `components/onboarding/OnboardingGate.tsx`, `PermissionSection.tsx`, `components/converter-wizard/WizardUploadStep.tsx`, `i18n/*.ts` |
| 2026-09-20 | Audio permission title and grant button were split into two lines leaving empty space | Layout put action in separate block row below header; user requested both on one line opposite each other | Integrated grant/settings action into the header flex row opposite the title, matching granted badge layout | `components/onboarding/PermissionSection.tsx` |
| 2026-09-20 | Albums and Liked tabs delayed/stuttered on first visit after cold start | Inactive tabs deferred mount until first click + KeepAlivePane returned null on first frame + AlbumGrid cols defaulted to 3 causing layout recalculation | Cooperative idle pre-warming via requestIdleCallback/setTimeout; synchronous first mount in KeepAlivePane; synchronous responsive column calculation | `components/music-player/KeepAlivePane.tsx`, `components/music-player/MusicPlayerView.tsx`, `components/music-player/AlbumGridVirtualized.tsx`, `__tests__/*` |
| 2026-09-21 | Android CI release build failed with 403 Forbidden downloading kotlin-stdlib/kotlin-dsl | Gradle buildSrc resolved plugins via Gradle Plugin Portal redirecting to repo.maven.apache.org which blocks CI runner IPs | Added buildSrc/settings.gradle.kts and updated settings.gradle with explicit google() and mavenCentral() plugin repositories | `scripts/patch-android-project.sh`, `src-tauri/gen/android/settings.gradle`, `src-tauri/gen/android/buildSrc/settings.gradle.kts` |
| 2026-09-21 | Android CI release failed with Kotlin Redeclaration on BuildTask / RustPlugin | Legacy buildSrc package dir com/audioconverter retained alongside com/audiflow | Migrated buildSrc sources to com/audiflow, cleaned legacy path in patch script and updated ProGuard rules | `src-tauri/gen/android/buildSrc/...`, `scripts/patch-android-project.sh`, `src-tauri/gen/android/app/proguard-rules.pro` |
| 2026-09-22 | Song switch kept previous track cover or lagged behind audio playback | Keyed on unstable ID/coverUrl leaving stale `extractedSrc` and `imgFailed` in state across changes; missing key on hero cover | Sync state reset in render on `trackIdentity` change + `key={trackIdentity(track)}` on hero cover | `utils/artwork.ts`, `TrackCover.tsx`, `NowPlayingView.tsx`, `TrackCover.test.tsx` |
| 2026-09-22 | New `audiflow` .deb refused to install over legacy `audio-converter` (dpkg overwrite error on `/usr/bin/ffprobe`) | Rebrand renamed the deb package but declared no transition; both packages ship same-named ffmpeg/ffprobe sidecars | `sudo apt remove audio-converter` unblocks now; `bundle.linux.deb` declares `conflicts`+`replaces: audio-converter` so future .debs upgrade cleanly | `src-tauri/tauri.conf.json` |
| 2026-09-22 | No song played on Linux: every track stuck at 0:00, all formats, no error output | WebKitGTK media backend cannot load audio from the `asset://` custom scheme (upstream WebKit bug 146351); element stalls at readyState 0 with neither play nor error | Linux-only: fetch asset bytes over `asset://` and hand the element a `blob:` URL (one live URL, revoked on next resolve/stop); fetch failure falls back to asset URL | `musicPlayer/linuxAssetAudio.ts` (new), `musicPlayer/audioEngine.ts`, `utils/platform.ts`, `__tests__/linuxAssetAudio.test.ts` (new) |
| 2026-09-22 | Trim/booster-A-B/ringtone previews silent on Linux (same 0:00 stall as main player) | `TrimEditor`, `FileBoosterInline`, `SetRingtoneModal` fed `asset://` straight into `<audio>` outside the player gate | Route all three through scoped `resolveScopedBlobAudioSrc` handles (owned revoke, never touches player singleton); fixed two stale `platform` test mocks missing `isLinux` | `TrimEditor.tsx`, `FileBoosterInline.tsx`, `SetRingtoneModal.tsx`, `linuxAssetAudio.ts`, `SetRingtoneModal.test.tsx`, `ConverterWizard.test.tsx` |
| 2026-09-22 | Fresh/minimal Ubuntu installs had no mp3/m4a/wma decode (silent skip, no hint) | `.deb` declared no `Depends`; minimal systems lack GStreamer ugly/bad/libav plugin sets | Explicit `depends`: webkit+gtk plus `gstreamer1.0-plugins-{base,good,bad,ugly}` and `gstreamer1.0-libav` (verified: Tauri writes `Depends` verbatim from config) | `src-tauri/tauri.conf.json` |
| 2026-09-22 | In-app updater handed Linux users AppImage (broken on Ubuntu 24.04+, no libfuse2) | `resolvePlatformDownloadUrl` picked first match regardless of format | Prefer `.deb` on Linux, AppImage as fallback; README (en/fa) recommends .deb + documents libfuse2 | `utils/githubUpdate.ts`, `githubUpdate.test.ts`, `README.md`, `README.fa.md` |
| 2026-09-22 | File picker looked hung on WMs without xdg-desktop-portal | `pickVideos` had no try/catch (unlike `pickDirectories`) → unhandled rejection | Fail-soft `[]` with portal comment, mirroring `pickDirectories` | `utils/dialog.ts` |
| 2026-09-22 | Blank/flickering window on NVIDIA + Wayland (WebKitGTK DMABUF vs driver) | No `WEBKIT_DISABLE_DMABUF_RENDERER` handling (tauri#9394) | Set it at startup only when NVIDIA hardware present (`/dev/nvidia0` or `/proc/driver/nvidia`) and user hasn't set it | `src-tauri/src/lib.rs` |
| 2026-09-22 | Transcribe API-key save failed cryptically on minimal/KDE Linux (no Secret Service) | Raw keyring/dbus error surfaced with no actionable hint | Linux-only hint appended: install gnome-keyring/kwallet + re-login | `src-tauri/src/secrets.rs` |
| 2026-09-22 | "Open With" file on Linux failed deep in ffmpeg job | `%U` passes `file://` URIs into argv; converter ingest never normalized (only player/library did) | `normalize_file_uri` (+ tiny `%XX` decoder, no new dep) at `start_queue`/`start_sound_boost` ingest; 5 Rust unit tests | `src-tauri/src/commands/mod.rs` |
| 2026-09-22 | "Open With Audiflow" missing for concrete types (freedesktop warning) | `fileAssociations.mimeType` used invalid `audio/*` / `video/*` wildcards | Concrete semicolon-separated MIME lists (mpeg/flac/wav/mp4/aac/ogg/opus/aiff/wma/webm + video set) | `src-tauri/tauri.conf.json` |
| 2026-09-22 | Linux lock screen showed only "Audiflow" instead of the song title | `asset://` artwork in `MediaMetadata` made WebKitGTK treat metadata as incomplete → useless MPRIS notification with app name only (WebKitGTK 2.52 MPRIS notes); empty titles also possible | Linux-only: omit non-http(s)/data/blob artwork from `MediaMetadata`; trim + non-empty fallbacks for title/artist/album | `utils/mediaSession.ts`, `utils/__tests__/mediaSession.test.ts` |
| 2026-09-22 | v1.5.4 CI red on all 3 OSes (6 fails in `linuxAssetAudio.test.ts`, green locally) | Stubs wrapped a jsdom `Blob` in undici `new Response(...)`, which brand-checks foreign Blob bodies and throws on CI's Node 22 (local Node 26 accepts) → fallback path taken | Duck-typed `{ok, blob()}` fakes; `FetchImpl` narrowed to that structural surface (real `fetch` still satisfies it) | `musicPlayer/linuxAssetAudio.ts`, `__tests__/linuxAssetAudio.test.ts` |
| 2026-09-22 | v1.5.5 CI red only on Ubuntu (3 fails in `TrimEditor.test.tsx`, green elsewhere) | Test never mocked `platform`: Ubuntu runner UA contains "Linux" → component took the async `fetch(asset://)` blob path that jsdom can't complete → `<audio>` never rendered | Pin `isLinux/isAndroid` to false in the component test (blob layer already covered in `linuxAssetAudio.test.ts`), matching the `SetRingtoneModal` precedent | `components/__tests__/TrimEditor.test.tsx` |
| 2026-09-22 | v1.5.6 mac release job failed at the new DMG-zip step (`zip I/O error`, exit 15) | `OUT_ZIP` was relative while the command ran after `cd "$STAGE"` (came in with the bulk commit, never executed locally) | Zip in place with `zip -q -j` from the workspace root, no staging dir; step proven locally with dummy DMG | `.github/workflows/release.yml` |
| 2026-09-23 | PKGBUILD package() could never find build output: `$srcdir/../..` resolves wrong under both BUILDIR modes (orchestrator: `packaging/arch/src-tauri/...` — nonexistent) | Path math assumed `$srcdir` sits two levels under repo root; makepkg `$srcdir` location varies with BUILDIR | Use makepkg's canonical `$startdir` (PKGBUILD dir = `packaging/arch`; `../../` = repo root in both modes) | `packaging/arch/PKGBUILD` |
| 2026-09-23 | First arch-in-release CI failed: `install: invalid option -- '7'` → makepkg exit 4 at "Package Arch Linux artifact" step | PKGBUILD line 67 `install -d755 "$pkgdir/usr/bin"` — GNU install clustered-short-option parsing fused the mode into the flag | Space the args: `install -d 755` (all other lines use `-Dm755/-Dm644` forms, fine) | `packaging/arch/PKGBUILD` |
| 2026-09-23 | `pnpm lint` failed with 9 errors/warnings (Fast Refresh rule, empty catch blocks, any cast, effect setState / deps) | Component exported helper alongside component; unannotated empty catches; explicit any cast; setState inside effects | Extracted useAlbumColumns; added best-effort comments; typed removeFile; synced state in render; added CI lint gate | `ToolSwitcher.tsx`, `AlbumGridVirtualized.tsx`, `useAlbumColumns.ts`, `TrackCover.tsx`, `FileBoosterPage.tsx`, `useFileBooster.ts`, `ci.yml` |
| 2026-09-24 | Newly transferred songs (USB OTG / PC) undetected on Android until external player opened | MediaStore does not auto-index raw storage writes; Audiflow only queried stale MediaStore rows without calling MediaScanner | Discovered audio files in Music/Download/OTG and synced via MediaScannerConnection before MediaStore query; broadened IS_MUSIC selection | `src-tauri/android/MediaScanSynchronizer.kt` (new), `src-tauri/android/MediaStoreManager.kt`, `MediaScanSynchronizerTest.kt` (new) |
| 2026-09-26 | CI & Release bundle builds crashed on all platforms at `pnpm build` (`tsc --noEmit`) | `src/types/__tests__/types.test.ts` mocked `InputFile` with non-existent `size` & `status` instead of `sizeBytes`, `durationSecs`, `formatName`, `error` | Updated `InputFile` mock properties and test assertions to match contract; verified `pnpm build` and `pnpm test` pass | `src/types/__tests__/types.test.ts` |
| 2026-09-26 | Android release build failed with E0599 (desktop WebviewWindowBuilder methods on Android) | Desktop floating overlay methods in `live_dubbing/overlay.rs` were compiled on Android where desktop window APIs are absent | Cfg-gated DesktopOverlayManager methods with `#[cfg(not(any(target_os = "android", target_os = "ios")))]` with no-op on mobile | `src-tauri/src/live_dubbing/overlay.rs` |
| 2026-09-26 | Android app crashed on click with UnsatisfiedLinkError (__gxx_personality_v0) | cpal was unconditionally listed in Cargo dependencies pulling oboe-sys C++ runtime into Android .so without libc++ | Cfg-gated cpal to non-mobile targets in Cargo.toml and player.rs; Android native .so now has 0 C++ runtime dependencies | `src-tauri/Cargo.toml`, `src-tauri/src/live_dubbing/player.rs` |


